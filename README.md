# 🧩 Resume Backend (Express + MongoDB)

This is the backend for **Tarun Kannadas' Resume Project**, built using **Node.js**, **Express**, and **MongoDB**.  
It provides RESTful APIs to store and retrieve resume data (like education, projects, and profile details), which can be fetched by the React frontend.

---

## 🚀 Features

- Connects to MongoDB using **Mongoose**
- RESTful API endpoints
- CORS enabled for frontend connection
- Express server setup with clean structure
- API ready for integration with React frontend

---
